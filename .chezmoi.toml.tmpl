{{- $codespaces := env "CODESPACES" | not | not -}}
{{- $remoteContainers := env "REMOTE_CONTAINERS_IPC" | not | not -}}
{{- $container := or $codespaces $remoteContainers -}}
[data]
  container = {{ $container }}
  email = "twpayne@gmail.com"
  name = "Tom Payne"
  sshKey = "twpayne@gmail.com SSH key"
