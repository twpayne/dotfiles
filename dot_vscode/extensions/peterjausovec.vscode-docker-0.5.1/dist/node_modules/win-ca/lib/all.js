// Generated by CoffeeScript 1.12.7

/*
Fetch all root CAs
 */
var all, crypto, der2, seen, sha1;

crypto = require('crypto');

der2 = require('./der2');

all = [];

module.exports = function(format) {
  return all.map(function(der) {
    return der2(format, der);
  });
};

sha1 = function(data) {
  return crypto.createHash('sha1').update(data, 'binary').digest('hex');
};

seen = {};

require('..').each(der2.der, function(der) {
  var z;
  if (!seen[z = sha1(der)]) {
    seen[z] = 1;
    all.push(der);
  }
});
