// Generated by CoffeeScript 1.12.7
var electron, isElectron;

if (process.platform !== 'win32') {
  return;
}

require('./format.oids');

isElectron = require('is-electron');

this.der2 = require('./der2');

this.electron = electron = isElectron();

this.each = !electron && (this.nApi = !!process.versions.napi) ? require('./each') : require('./each.fallback');

this.all = require('./all');

require('./inject');

this.path = require('./save').path;
