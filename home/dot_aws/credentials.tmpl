[default]
aws_access_key_id = {{ (onepasswordDetailsFields "AWS Access Key" .vault).username.value }}
aws_secret_access_key = {{ (onepasswordDetailsFields "AWS Access Key" .vault).password.value }}
[terraform]
aws_access_key_id = {{ (onepasswordDetailsFields "AWS Terraform Access Key" .vault).username.value }}
aws_secret_access_key = {{ (onepasswordDetailsFields "AWS Terraform Access Key" .vault).password.value }}
